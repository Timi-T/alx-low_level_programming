#include "main.h"
#include <stdio.h>
/**
 * int _isupper - function to check case
 *
 * Return: Always 0
 *
 * @c:
 */
int _isupper(int c)
{
	int i;

	for (i = 'A'; i <= 'Z'; i++)
	{
		if (c = i)
		{
			return (1);
		}
		else
		{
			return (0);
		}
	}
	return (0);
