#include "main.h"

/**
 * _strlen - function to print length of string
 *
 * @s: pointer for the string
 *
 * Return: always 0
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{

	}
	return (i);
}
